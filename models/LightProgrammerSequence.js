

// Time in ms
// TODO Sequence runner to read, iterate through sequence based on time.
// Sequence runner could detect longer frame times using millis? and display long/repair frames

class LightProgrammerSequence {

	constructor() {
		this._sequence = [	// TODO remove times.. this will just be an array of 0s and 1s
			{
				time: 100,
				value: 1
			}
		];
		this._sequencePrefix = [];
		this._sequenceSuffix = [];
	}

	/**
	 * Returns the full squence?
	 */
	getSequence() {
		
	}

	/**
	 * Returns the sequence item at index.
	 * 
	 * @param {*} index 
	 */
	getAt(index) {
		
	}

	/**
	 * 
	 */
	getLength() {
		
	}

}